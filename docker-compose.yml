version: "3.3"
services:
  docker-cpp-template:
    build:
      context: .
      dockerfile: Dockerfile
    image: docker-cpp-template
    container_name: docker-cpp-template
    volumes:
      - type: bind
        source: .
        target: /workspace
    stdin_open: true
    tty: true
    working_dir: /workspace
